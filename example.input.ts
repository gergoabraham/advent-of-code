/**
 * Copy this file next to the source files with the correct file name, e.g.
 * ./src/13/13.input.ts
 *
 * and assign the puzzle input to the `input` variable.
 */

const input = ``;

export default input;
